layerrule = match:namespace shell-.*, blur on, blur_popups on, ignore_alpha 0.2, no_anim on
layerrule = match:namespace shell-bar, xray on
layerrule = match:namespace shell-close, blur off
exec = systemctl --user start shell@reload
